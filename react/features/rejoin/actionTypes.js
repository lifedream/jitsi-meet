/**
 * The type of (redux) action which signals that the user has pressed the hangup button.
 */
export const WILL_HANGUP = 'WILL_HANGUP';

/**
 * The type of (redux) action which stores start time of the current conference.
 */
export const SET_START_TIME = 'SET_START_TIME';

/**
 * The type of (redux) action which stores the end time of the current conference.
 */
export const SET_END_TIME = 'SET_END_TIME';
